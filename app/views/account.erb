<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(@session) %>
      <h3>Username: <%= Helpers.current_user.username %> Balance: <%= Helpers.current_user.balance %></h3>
      <p><a href="/logout">Log Out</a></p>
    <% else %>
  </body>
</html>
